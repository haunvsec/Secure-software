# Secure Software Board — Environment Variables

# MariaDB
DB_HOST=localhost
DB_PORT=3306
DB_USER=cvedb
DB_PASSWORD=cvedb
DB_NAME=cve_database
DB_ROOT_PASSWORD=rootpass

# App
APP_PORT=5005

# Scheduler
SYNC_INTERVAL_HOURS=1
DISABLE_SCHEDULER=false

# SQLite path (for migration only)
SQLITE_PATH=cve_database.db
